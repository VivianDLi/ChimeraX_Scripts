<!--
ChimeraX bundle names must start with "ChimeraX-"
to avoid clashes with package names in pypi.python.org.
When uploaded to the ChimeraX toolshed, the bundle
will be displayed without the ChimeraX- prefix.
-->

<BundleInfo name="ChimeraX-VolumeDistanceCommand"
            version="0.1.0"
            package="chimerax.volume_distance_cmd">

    <Author>Vivian Li</Author>
    <Email>vdl21@cantab.ac.uk</Email>
    <!--Add GitHuB <URL>-->
    <Synopsis>Calculate the shortest distance between distinct sections of two volumes.</Synopsis>
    <Description>This command calculates the shortest distance between distinct sections of two volumes. This is useful for determining the distances between elements of two segmentation volumes to view statistically, for example, to produce a histogram of the shortest distance between nuclear pores and nearby mitochondria.</Description>

    <!-- Categories is a list where this bundle should appear -->
    <Categories>
        <Category>Volume</Category>
    </Categories>

    <!-- DataFiles is a list of additional files to include in the bundle -->
    <DataFiles>
        <DataFile>docs/user/commands/tutorial.html</DataFile>
    </DataFiles>

    <!-- Dependencies on other ChimeraX/Python packages -->
    <Dependencies>
        <Dependency name="ChimeraX-Core" version=">=1.1"/>
        <Dependency name="numpy" version=">=1.20"/>
        <Dependency name="scipy" version=">=1.6"/>
    </Dependencies>

    <Classifiers>
        <!-- Development Status should be compatible with bundle version number -->
        <PythonClassifier>Development Status :: 3 - Alpha</PythonClassifier>
        <PythonClassifier>License :: Freeware</PythonClassifier>
        <!-- ChimeraX classifiers describe supplied functionality -->
        <ChimeraXClassifier>ChimeraX :: Command :: volume distance :: Volume :: Calculate distances between distinct sections of two volumes</ChimeraXClassifier>
    </Classifiers>
</BundleInfo>